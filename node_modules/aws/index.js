//  TODO: each service needs scope and resource usage report
//        resource usage reports include details and prices

var AWS = require('aws-sdk');
var credentials = require('credentials');

var Bucket = 'files';

var AWSconfig = {
    accessKeyId     : credentials.aws.access_key,
    secretAccessKey : credentials.aws.secret_key,
    region : 'us-west-2'
};

AWS.config.update(AWSconfig);

module.exports = {
    dynamodb : require('./dynamodb')(AWS),
    ses      : require('./ses')(AWS),
    s3       : require('./s3')(AWS)
};
