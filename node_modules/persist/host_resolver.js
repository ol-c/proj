var resolver = null;

function host_resolver(fn) {
    resolver = fn;
}

module.exports.set = host_resolver;
module.exports.get = function (id, callback) {
    if (resolver) {
        resolver(id, callback)
    }
    else {
        callback(null, null);
    }
}
