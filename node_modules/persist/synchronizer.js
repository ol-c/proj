var persist = require('persist');

/*
    A synchronizer is responsible for maintaining a mapping of
    perisistant object ids and the hostname for the process that
    has locked it

    it also sets up the s3 location to save to
*/
