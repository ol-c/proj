var vm = require('vm');
var o = require('o');

function scoped(expression) {
    vm.runInNewContext('var _tmp_ = ' + expression, o);
    var value = o._tmp_;
    delete o._tmp;
    return value;
}

module.exports = scoped;
