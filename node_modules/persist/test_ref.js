var references = require('./references');

var p = references.create('1393742111497456400');

function resolved(val)   { console.log('RESOLVED', val);   }
function rejected(error) { console.log('REJECTED', error); }

var promise = references.resolve(p);
promise.then(resolved, rejected);
