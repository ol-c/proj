module.exports = unhash_reference;

function unhash_reference(hashed_reference) {
    var id = hashed_reference.split('.').shift();
    var internal = hashed_reference.substr()
    return {
        id : id,
        internal : internal
    };
}
